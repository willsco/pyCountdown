```
countdown_app/
│
├── app.py                      # Ponto de entrada da aplicação
├── controllers/
│   └── CountdownController.py  # Controlador que lida com a rota e a lógica
├── models/
│   └── CountdownTimer.py       # Modelo que faz o cálculo do tempo
└── views/
    └── CountdownGif.py         # Visão que gera e retorna o GIF

```
